const {
    todos,
    populateTodos
} = require('./todoSeed');

const {
    users,
    populateUsers
} = require('./userSeed');
module.exports = {
    todos,
    populateTodos,
    users,
    populateUsers
}